@model IEnumerable<HospitalSchedule.Models.Nurse_Schedule>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schedule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nurse)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Schedule.ScheduleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nurse.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Nurse_ScheduleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Nurse_ScheduleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Nurse_ScheduleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
