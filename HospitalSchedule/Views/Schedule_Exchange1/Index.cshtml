@model IEnumerable<HospitalSchedule.Models.Schedule_Exchange1>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schedule)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Schedule.ScheduleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Schedule_Exchange1Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Schedule_Exchange1Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Schedule_Exchange1Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
