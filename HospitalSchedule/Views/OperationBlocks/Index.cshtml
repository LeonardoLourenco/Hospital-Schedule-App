@model IEnumerable<HospitalSchedule.Models.OperationBlock>

@{
    ViewData["Title"] = "Index";
}

<h2>Blocks List</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BlockName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BlockName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OperationBlockId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OperationBlockId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OperationBlockId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
