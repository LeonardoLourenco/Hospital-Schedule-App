@model IEnumerable<HospitalSchedule.Models.Rules>

@{
    ViewData["Title"] = "Index";
}

<h2>Rules List</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RulesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RulesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RulesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
