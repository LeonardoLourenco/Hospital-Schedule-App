@model IEnumerable<HospitalSchedule.Models.Shift_Schedule>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShiftDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schedule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shift)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShiftDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Schedule.NurseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shift.ShiftId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Shift_ScheduleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Shift_ScheduleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Shift_ScheduleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
