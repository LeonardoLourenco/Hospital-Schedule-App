@model HospitalSchedule.Models.Specialty

@{
    ViewData["Title"] = "Speciality's Details";
}

<h2>Speciality's Details</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@Model.SpecialtyId">Edit</a>
    <a asp-action="Index" class="btn btn-primary">Back to List</a>
</div>
